* generate & copy /.ssh/id_rsa.pub to ~/.ssh/authorized_keys (verify rights) in order to labgrid to work !! 

* add README.md with labgrid help to the image (home folder)
* solve netbird first login problem.
* chmod /etc/labgrid to grant user's access read files exporter & coordinator
* esp-idf install is failing..
* add all developers pubkeys to known host (as our fw docker do)

